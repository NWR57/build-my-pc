module.exports = {
    description: "Get the websocket ping of the message.",
    run: (client, message) => {
        return message.reply(`This feature is currently not available. Sorry for the inconvenience`)
    }
};